# Google Cloud Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# GCP Project ID
# Example: central-element-323112
GCP_PROJECT_ID=

# Path to Google Cloud service account key file (JSON)
# Default: .secrets/gcs/gcs-service-account.json (relative to project root)
# For Docker: /app/.secrets/gcs/gcs-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=.secrets/gcs/gcs-service-account.json

# Unified Cloud Services GCS Bucket (contains Parquet files)
# This is the main bucket for market data and instruments
# Example: market-data-tick-cefi-central-element-323112
UNIFIED_CLOUD_SERVICES_GCS_BUCKET=

# GCS Bucket for backtest results
# Example: execution-store-cefi-central-element-323112
GCS_BUCKET=

# Optional: Additional bucket configurations
# Instruments bucket (if different from main bucket)
# INSTRUMENTS_BUCKET=instruments-store-cefi-central-element-323112

# Optional: Market data bucket (if different from main bucket)
# MARKET_DATA_BUCKET=market-data-tick-cefi-central-element-323112

# Optional: Results bucket (if different from main bucket)
# RESULTS_BUCKET=execution-store-cefi-central-element-323112

# UCS Configuration
# Set to true to use direct GCS access (instead of FUSE mount)
UNIFIED_CLOUD_SERVICES_USE_DIRECT_GCS=false

# Local path for data (used when FUSE mount is available)
# UCS auto-detects FUSE mounts, so this is mainly for fallback
UNIFIED_CLOUD_LOCAL_PATH=/app/data_downloads

# Use Parquet format (default: true)
UNIFIED_CLOUD_SERVICES_USE_PARQUET=true

# Data catalog path (for NautilusTrader catalog)
DATA_CATALOG_PATH=/app/backend/data/parquet

# GCS FUSE Configuration (optional)
# Set to true to enable GCS FUSE mounting
USE_GCS_FUSE=false

# GCS FUSE bucket name (if using FUSE mount)
# GCS_FUSE_BUCKET=

# GCS Service Account Key (for FUSE mounting, if different from GOOGLE_APPLICATION_CREDENTIALS)
# Default: .secrets/gcs/gcs-service-account.json
# GCS_SERVICE_ACCOUNT_KEY=.secrets/gcs/gcs-service-account.json
